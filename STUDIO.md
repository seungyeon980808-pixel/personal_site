# 작업실 실행과 관리

현재 브랜치: `codex/interactive-desktop`.

```sh
npm ci
npm run build
npm run preview:studio
```

홈: http://localhost:4321/ · 기존 목록형 사이트: http://localhost:4321/classic.html

## 방문자 화면
- 흰 배경의 닫힌 노트북을 누르면 문구가 옅어지고 뚜껑이 열린 뒤 화면으로 확대됩니다. 다음 페이지에서 돌아오면 같은 탭의 바탕화면이 유지됩니다.
- 캘린더 날짜에는 그날의 생각·개발일지·연수가 함께 표시됩니다. 전체 기록도 캘린더에서 엽니다.
- Dock에는 만든 프로그램 폴더·캘린더·설정이 있으며, 폴더 안에 기존 공개 프로그램 7개가 있습니다. 상세 페이지·기존 댓글/영상 기능은 유지됩니다.
- 추천 도구함/연수 자료는 분야 → 폴더 → 자료로 탐색합니다. 자료 링크와 스킬 설치 안내를 등록할 수 있습니다.
- 창 제목줄을 끌어 이동합니다. 빨강 닫기, 노랑 최소화, 초록 확대, 화살표 뒤로 가기를 지원합니다. 최소화한 창은 Dock 위 버튼으로 복원합니다.
- 검색 또는 Command/Ctrl+K로 프로그램·기록·자료·바로가기를 검색합니다.

## 관리 흐름
1. Dock의 **설정** 아이콘에서 관리 화면과 초안을 엽니다. 다른 방문자에게 보이지 않는 브라우저 로컬 초안입니다.
2. 메모, 기록, 추천·연수 자료, 바로가기를 추가/수정/삭제합니다. 분야와 폴더 이름을 입력하면 폴더가 만들어집니다. 자료마다 Google Drive, 스프레드시트, 웹페이지, 다운로드 URL 등을 연결할 수 있습니다.
3. 직접 고른 PNG/JPEG/WebP 아이콘은 128px WebP로 축소됩니다. 원본은 10MB까지, 작업실 전체 데이터는 800KB까지 허용합니다.
4. **초안 저장** 후 **Google 관리자 로그인**을 누르고 기존 관리자 계정으로 로그인합니다.
5. **공개 게시**를 누르면 `personal-site/desktop-v1` 문서에 저장되어 새로 방문/새로고침한 화면에 반영됩니다. 게시 전 확인창이 나옵니다.
6. **공개 화면 보기**로 로컬 초안과 방문자 화면을 비교할 수 있습니다. **초안 버리기**는 로컬 초안만 제거하며 공개 내용은 지우지 않습니다.
7. JSON 백업을 내보내고 다시 가져올 수 있습니다. 프로그램·소개·채널은 관리 화면의 **프로그램·소개 편집**에서 기존 편집기를 사용합니다.

## 데이터와 권한
- 기존 `personal-site/main` 문서의 프로그램, 채널, 작업 흐름을 읽습니다. 첫 게시 전 기존 생각·개발일지·추천 자료를 새 작업실의 초기 콘텐츠로 사용합니다.
- 첫 게시 이후 기록·추천·연수·메모·바로가기는 `desktop-v1`이 원본입니다. 기존 목록형 페이지의 기록과 양방향 동기화하지 않습니다. 프로그램·소개·채널은 계속 main에서 읽습니다.
- Firebase 기존 관리자 규칙을 그대로 사용합니다. 클라이언트의 버튼 숨김만으로 권한을 결정하지 않으며, 서버가 쓰기를 허용해야 게시됩니다.
- 로그인 SDK는 로그인할 때만 내려받습니다. 방문자는 REST batchGet 1회로 공개 내용만 읽습니다.
- 다른 기기에서 먼저 수정한 경우 updateTime 사전조건으로 덮어쓰기를 막습니다. 초안을 백업하고 공개 내용을 다시 읽은 뒤 적용합니다.
- Google 인증 팝업/실제 공개 쓰기는 소유자 계정으로 최종 확인이 필요합니다. 자동 테스트는 가짜 인증·응답으로 격리하여 실제 Firestore에 쓰지 않습니다.
- 신규 연수 파일 자체의 업로드 저장소는 추가하지 않았습니다. 기존 공유 드라이브/파일 링크를 연결하는 방식입니다. 링크 대상의 공유 권한은 해당 서비스에서 설정합니다.

## 소스와 빌드
`src/studio/`는 편집용 소스이며, `npm run build`가 CSS/JS를 인라인으로 합쳐 단일 `index.html`을 만듭니다. `index.html`을 직접 수정하지 마세요. 기존 페이지는 `classic.html`, 상세는 `projects/`에 있습니다.

- `npm run assets:studio`: 승인된 시안의 교실·호수 원본을 WebP로 변환
- `npm run test:studio`: 실제 Chrome for Testing으로 UI·저장·권한·모바일 테스트
- `npm run audit:studio`: 실제 Chrome에 연결한 Lighthouse Node API 측정
- 최초 테스트 브라우저 설치: `npx puppeteer browsers install chrome`
- 캡처 및 테스트 리포트: `.omo/evidence/studio/` (공개 배포 제외)

이번 작업은 로컬 브랜치 구현입니다. GitHub push, main 병합, Firebase/GitHub Pages 배포, 실제 공개 콘텐츠 변경은 수행하지 않았습니다.

## 진입과 프로그램 창
- 노트북 화면에는 사진 속 예시 UI 대신 실제 `#desktop`을 축소해 넣습니다. 진입 완료 시 같은 DOM을 본문으로 옮기므로 배경·메모·프로그램·캘린더 위치와 내용이 유지됩니다. 바깥에서는 inert로 조작을 막고, 안으로 들어오면 활성화합니다.
- 마우스를 올리거나 키보드로 선택하면 뚜껑이 조금 열립니다. 클릭은 현재 각도에서 이어지며 동작 줄이기 설정에서는 이 반응을 생략합니다.
- 최신 버전은 열린 기준 컷과 여기서 파생한 닫힌 컷을 사용합니다. 본체는 열린 컷의 동일한 영역 하나로 유지하고, 화면 영역만 힌지를 중심으로 회전합니다. 기존 전체 사진 크로스페이드와 별도 CSS 노트북은 제거했습니다. 열림 1100ms → 열린 컷 320ms → 화면 진입 800ms입니다.
- 최신 입구는 승인된 닫힌/열린 이미지의 노트북 부분을 그대로 사용합니다. 두 이미지 사이의 짧은 열림 구간만 CSS 하드웨어로 연결하고, 마지막에는 실제 작업실로 전환합니다. 원본 주변 문구는 이미지 파일에서 제외했습니다.
- 문구·48px 간격(모바일 36px)·닫힌 노트북 전체 높이를 측정해 화면 중앙에 배치합니다. 열림 중에는 열린 이미지의 중앙 구도로 이동합니다.
- 노트북은 낮은 정면 시점에 고정되며 960ms 동안 뚜껑이 열리고, 열린 상태를 400ms 보여준 뒤 760ms 동안 화면으로 진입합니다. 본체와 카메라는 열림 구간에서 이동하지 않습니다.
- `assets/studio/notebook-deck.webp`는 생성한 키보드·금속 표면 소재입니다. 화면 속 내용은 실제 HTML로 유지하며 유료 3D 모델이나 영상은 사용하지 않습니다.
- 닫힌 노트북 자체를 클릭하거나 키보드로 선택해 들어갑니다. 별도의 안내 버튼은 표시하지 않습니다.
- 상단 ‘노트북 닫기’ 또는 열린 창의 ‘닫고 나가기’로 입구로 돌아갑니다. 동작 줄이기 설정에서는 확대와 회전을 생략합니다.
- 프로그램에서 ‘설명 보기’와 ‘바로 실행’을 선택하면 작업실 안의 브라우저 창으로 열립니다. 외부 서비스가 포함을 막거나 로그인이 필요하면 ‘별도 창’으로 엽니다.
- 브라우저 창을 최소화했다 복원해도 화면을 다시 불러오지 않습니다.
- 편집 중 ‘새 바로가기’는 기존 앱 아이콘과 구별되는 점선 패널입니다.

### 2026-09-12 노트북 힌지 수정
- `notebook-refined-open.webp` / `notebook-refined-closed.webp`: 기존 제품 시안을 참조해 생성한 이미지. 같은 하부 사진을 유지하면서 금속 가장자리와 키보드 디테일을 보완했습니다.
- 호버는 상판 뒤쪽 축을 고정하고 앞쪽 투영 깊이만 바꿉니다. 상판 전체를 위로 옮기는 동작은 제거했습니다.
- 실제 화면 DOM 연결과 밝은 배경은 유지합니다. 힌지 좌표, 입장 전후 배치, 느린 이미지 로딩, 입장 취소·복귀를 검증했습니다.
